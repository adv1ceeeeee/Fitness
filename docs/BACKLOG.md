# Sportify — Бэклог

Задачи, которые осознанно отложены. Не баги и не срочные фичи —
всё это сделаем когда придёт время.

> Актуально на: 2026-04-18

---

## Геймификация / Pro-косметика

### Pro-множитель XP (×1.5)
**Где:** `GamificationService.award()`
**Текущее состояние:** XP начисляется одинаково всем пользователям.
**Что нужно:**
- Поле `profiles.is_pro` (см. раздел «Paywall / Pro-подписка» ниже)
- В `award()` домножать `finalAmount` на 1.5 для Pro-пользователей
- Визуальный индикатор «×1.5 XP» рядом с бейджем уровня

### Pro-рамки и эффекты бейджа уровня
**Где:** `_LevelBadge` в `profile_screen.dart`
**Текущее состояние:** Бейдж одинаковый для всех.
**Что нужно:**
- Анимированная рамка / glow-эффект для Pro-пользователей
- Уникальные цвета/градиенты бейджа для Pro

### Сезонные награды
**Где:** — (новый экран)
**Текущее состояние:** Сезонный decay XP (50% каждые 6 мес) уже реализован, но наград за максимальный уровень в сезоне нет.
**Что нужно:**
- Таблица `season_rewards` (user_id, season_start, max_level, reward_type)
- Запись при decay: сохранить max level достигнутый за сезон
- UI: коллекция сезонных наград в профиле
- Возможные награды: иконки, рамки аватара, титулы

### Лидерборд (локальный/глобальный)
**Что нужно:**
- View или materialized view `xp_leaderboard` (top 100 по xp_total)
- Экран лидерборда с аватарами и уровнями
- Возможно: фильтр по друзьям / по городу
- **Когда делать:** после набора >500 активных пользователей

---

## UX / Персонализация

### Настраиваемые кнопки «Быстрые действия» на главном экране
**Где:** `HomeScreen` → секция «Быстрые действия»
**Текущее состояние:** 4 фиксированные карточки (2×2).
**Что нужно:**
- UI выбора/сортировки (drag-and-drop или чекбоксы)
- Хранение конфигурации в SharedPreferences
- Поддержка разных размеров карточек (1×1, 2×1)

**Когда делать:** когда разделов станет заметно больше 4, или аналитика покажет что текущий набор не устраивает пользователей.

---

## Безопасность

### Улучшить хеширование PIN: HMAC → PBKDF2
**Где:** `lib/services/pin_service.dart` → `_hashPin`
**Текущее состояние:** Итерированный HMAC-SHA256 (ключ = userId, 1000 раундов).
Слабость: ключ предсказуем (userId известен), нет соли.
**Что нужно:**
- Добавить пакет `pointycastle` или `cryptography`
- Генерировать случайную соль при `setupPin`, хранить рядом с хешем в FlutterSecureStorage
- Использовать PBKDF2 (100 000 итераций, SHA-256)
- Миграция существующих PIN: при следующем успешном входе пересохранить в новом формате

### HTTPS Certificate Pinning
**Где:** сетевой слой (Supabase клиент)
**Текущее состояние:** Нет пиннинга — MITM-атака возможна на устройстве с установленным CA.
**Что нужно:**
- Получить fingerprint сертификата Supabase
- Настроить `HttpClient` с кастомным `SecurityContext` или использовать пакет `ssl_pinning_plugin`
- Актуально главным образом для продакшн-сборки (не dev)

---

## Функциональность

### Социальная авторизация (OAuth)
**Где:** `WelcomeScreen` / `LoginScreen` / `RegisterScreen`
**Текущее состояние:** Только email + пароль.
**Приоритет для РФ:**
1. **Яндекс ID** — custom OIDC provider в Supabase Dashboard; самый широкий охват
2. **Apple Sign In** — обязателен на iOS при наличии любого другого соц. входа (App Store Guideline 4.8)
3. **Google** — из коробки в Supabase (`signInWithOAuth`), но не работает на устройствах без GMS
4. **VK ID** — факультативно, если целевая аудитория 18–35
**Что нужно:**
- Supabase Dashboard: OAuth client ID + secret для каждого провайдера
- Flutter: `supabase.auth.signInWithOAuth()` + пакет `google_sign_in` для Google; custom deep link для Яндекс
- Onboarding-флаг: новый пользователь через OAuth → редирект на `/onboarding` если профиль не заполнен
- `profiles.nickname` / `full_name` автозаполняются из данных провайдера при первом входе

---

### Верификация контактного email (OTP-код)
**Где:** `EditProfileScreen`
**Текущее состояние:** Email меняется без подтверждения — можно указать чужой адрес.
**Что нужно:**
- Аккаунт Resend + API key + верифицированный домен
- Edge Function `send-email-otp`: генерирует 6-значный код, хранит в `email_verifications` (TTL 10 мин), шлёт письмо
- Миграция `050_email_verifications.sql`
- UI: диалог ввода кода в `EditProfileScreen`

### iOS WidgetKit виджеты
**Где:** нативный Swift код
**Текущее состояние:** Не реализовано.
**Что нужно:**
- Swift-таргет в Xcode с WidgetKit
- Shared App Group для передачи данных Flutter → виджет
- Варианты: streak, следующая тренировка, последний вес

### Paywall / Pro-подписка
**Где:** — (новый экран)
**Текущее состояние:** Монетизация не реализована, весь функционал бесплатен.
**Что нужно:**
- Интеграция RuStore Billing или Google Play Billing (`purchases_flutter` / RevenueCat)
- Определить какие фичи за paywall
- Экран подписки + restore purchases
- Поле `profiles.is_pro` + RLS на premium-контент

### Пригласить тренера
**Где:** `ProfileScreen`
**Текущее состояние:** Плейсхолдер в UI.
**Что нужно:**
- Таблица `trainer_client` (социальные связи)
- Invite-флоу через deep link
- Роль тренера: просмотр истории клиента, комментарии к сессиям

---

## Инфраструктура

### A/B тестирование
**Текущее состояние:** Нет механизма деления на группы.
**Что нужно:**
- Поле `profiles.experiment_group` (случайное при регистрации)
- EventLogger автоматически добавляет `experiment_group` в props каждого события
- Анализ по когортам в Metabase / SQL

### Supabase CLI + миграции в CI
**Текущее состояние:** Миграции применяются вручную через Dashboard.
**Что нужно:**
- `supabase db push` в CI/CD pipeline после merge в main
- Секрет `SUPABASE_ACCESS_TOKEN` в GitHub Actions
