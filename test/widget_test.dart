// Widget integration tests require a running Supabase instance and are
// therefore excluded from the unit-test suite. See test/models/,
// test/providers/, and test/services/ for the unit tests.

import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — widget tests that need Supabase are run in integration tests.
  test('placeholder: widget test suite loads successfully', () {
    expect(true, isTrue);
  });
}
